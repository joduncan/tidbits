import std.stdio;
import std.array;
import std.algorithm;

int main(string[] args)
{
    int array_size = to!int(chomp(stdin.readln()));
    int primary_sum = 0;
    int secondary_sum = 0;
    foreach(index; iota(array_size))
    {
        primary_sum += blah[1][1];
        secondary_sum += blah[1][1];
    }
}
